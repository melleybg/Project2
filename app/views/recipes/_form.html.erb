<%= form_for @recipe do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :img_url %>
  <%= f.text_field :img_url %>

  <%= f.label :detail %>
  <%= f.text_field :detail %>

  <%= f.submit %>

<% end %>

<!-- definitely remove the code below. since this file is a partial (part of a layout), this will cause a 2nd body to be inserted into the HTML document and potentially   invalidate it -->

<body>
  <input placeholder="Search">
</body>
